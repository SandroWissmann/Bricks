#ifndef UTILITY_ISNUMBER_H
#define UTILITY_ISNUMBER_H

#include <string>

namespace bricks::utility {

template <typename T> bool isNumber(const std::string& s);

} // namespace bricks::utility

#endif